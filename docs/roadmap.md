# SceneScope 项目路线图

> 本文是项目进度、当前任务和 Backlog 的唯一状态来源。

## 1. 执行原则

- 一次只保留一个“进行中”任务。
- 日期是节奏参考；里程碑未关闭时，不假装进入下一阶段。
- 遇到困难先缩小功能，不删除验证，也不为保留全部功能无限延期。
- 新想法先进入 Backlog，并回答它是否帮助正确展示资产、发现资产问题或形成工程证据。
- 三项均为“否”时移除；只有一项为“是”时默认排到 v0.1 之后。

状态只使用：`待办`、`就绪`、`进行中`、`阻塞`、`完成`、`移除`。

## 2. 当前阶段：M2 从查看器到检查器

目标：从当前单 mesh 演示模型演进到能够表达节点、网格、材质、纹理和统计的内部资产模型，并让 Windows/Web 从用户选择的 GLB 字节加载资产。

M0 和 M1 已关闭。M1 的可复现证据包括：

- `scenescope-gltf` 的 Box 解析测试验证 24 个顶点、36 个索引、法线和节点世界变换。
- Windows 与 Web 共用 `MeshInstance` 和 `scenescope-render`，能够显示同一份 `Box.glb`。
- 渲染器包含深度测试、背面剔除、sRGB 输出和轨道相机。
- Windows 运行截图见 [M1 桌面端截图](images/scenescope-m1-desktop.png)。
- `cargo release-check`、Wasm target 检查和 `wasm-pack build` 已通过。

| ID | 优先级 | 任务 | 验收标准 | 状态 |
|---|---|---|---|---|
| M2-001 | P0 | 建立 v0.1 内部 `AssetDocument` 和资产统计 | Core 类型不依赖 glTF/wgpu；能够表达 v0.1 所需节点、mesh primitive、材质、纹理引用和确定性统计 | 进行中 |
| M2-002 | P0 | 扩展 GLB 转换与渲染输入 | 支持多个节点/mesh primitive、UV 和 Base Color 纹理；缺失可选属性有明确语义 | 待办 |
| M2-003 | P0 | 接入 Windows/Web 本地文件选择 | 两端都以字节调用同一转换入口；取消选择不报错 | 待办 |
| M2-004 | P0 | 显示节点树、资源摘要和统计 | 用户可浏览节点、网格、材质、纹理及核心统计 | 待办 |
| M2-005 | P0 | 分类加载错误并增加回归资产 | 损坏、空或不支持输入不崩溃；多个 Khronos 资产通过回归检查 | 待办 |

下一步：先根据解析、渲染两个现有消费者和 M2 已明确的统计需求设计 `AssetDocument` 的最小数据形状，再由项目作者实现 Core 类型和测试。

## 3. 里程碑

周期从 M0 关闭后开始计算。

| 里程碑 | 参考周期 | 唯一结果 | 状态 | 完成证据 |
|---|---|---|---|---|
| M0 启动 | 第 1 天 | 可运行的最小工程 | 完成 | workspace、范围文档、固定 Box、窗口清屏/退出 |
| M1 跨平台第一帧 | 第 1～2 周 | 同一 Box 在 Windows/Web 正确显示 | 完成 | 解析测试、双端运行证据、共享场景数据、运行截图 |
| M2 查看器到检查器 | 第 3～4 周 | 能理解资产结构和加载失败原因 | 进行中 | 内部模型、树/统计/纹理、多个回归资产 |
| M3 规则与报告 | 第 5～6 周 | 可自动化地产生一致诊断 | 待办 | 5 条规则、规则测试、CLI JSON、一致性测试 |
| M4 工程化发布 | 第 7～8 周 | 可公开使用的 `v0.1.0` | 待办 | CI、性能基线、失败路径、Windows/Web 发布 |
| M5 项目案例整理 | 第 9～12 周 | 项目问题、方案和证据可快速理解 | 待办 | README、演示、ADR、性能与调试案例 |
| M6 平台扩展 | v0.1 后 | 按证据扩展平台 | 待办 | macOS/Linux -> Android -> iOS 的真实环境验证 |

## 4. 里程碑完成标准

### M1：跨平台第一帧

- 从 Box GLB 字节提取首个静态 indexed primitive。
- 上传顶点和索引，完成基础 shader、深度测试和相机。
- 处理节点 TRS，渲染方向和比例正确。
- Windows 与 Web 使用同一份内部场景数据。
- 至少一个自动化解析测试。
- README 包含运行命令、截图、限制和已知问题。

### M2：从查看器到检查器

- 建立不绑定 glTF crate 的内部资产模型。
- 显示节点树、资源摘要和资产统计。
- 支持法线、UV 和 Base Color 纹理。
- Windows/Web 均能从用户选择的本地 GLB 读取字节；拖放可后补。
- 损坏、空或不支持的输入返回有分类的错误。
- 使用多个 Khronos Sample Assets 做回归测试。

### M3：规则与报告

- 实现 InspectionProfile、Diagnostic 和静态规则组合。
- 完成至少 5 条检查规则，每条有通过和失败测试。
- CLI 读取 GLB 并输出 JSON 报告。
- GUI、Web 和 CLI 的统计与诊断一致。
- 报告包含规则 ID、严重程度、对象位置、说明和建议。

### M4：工程化与发布

- 建立格式化、静态检查、测试和多目标构建检查。
- 对核心解析和规则执行建立 benchmark 基线。
- 使用 CPU profiler 或 RenderDoc 完成一次有证据的分析。
- 覆盖大文件、空场景、缺失属性和不支持扩展等失败路径。
- 发布 Windows 程序、Web Demo 和 `v0.1.0` 说明。

### M5：项目案例整理

- README 能在 60 秒内说明问题、方案、架构和运行方式。
- 提供 60～90 秒演示视频或 GIF。
- 保留架构决策、性能数据和一个完整调试案例。
- 技术文章只来自项目中实际解决的问题。

### M6：平台扩展

顺序固定为 Windows + Web、macOS + Linux、Android、iOS。没有经过真实环境构建和运行验证的平台，不得声明支持。

## 5. Backlog

| ID | 里程碑 | 优先级 | 任务 | 状态 |
|---|---|---|---|---|
| M1-001 | M1 | P0 | 从固定 Box.glb 提取首个 indexed primitive | 完成 |
| M1-002 | M1 | P0 | Windows 渲染 Box | 完成 |
| M1-003 | M1 | P0 | 节点 TRS、深度、剔除和轨道相机 | 完成 |
| M1-004 | M1 | P0 | Web 第一帧并验证共享场景数据 | 完成 |
| M2-001 | M2 | P0 | 内部 AssetDocument、节点树和统计 | 进行中 |
| M2-002 | M2 | P0 | 多节点/primitive、UV 和 Base Color 纹理 | 待办 |
| M2-003 | M2 | P0 | Windows/Web 本地文件选择 | 待办 |
| M2-004 | M2 | P0 | 节点树、资源摘要和统计界面 | 待办 |
| M2-005 | M2 | P0 | 错误分类与多个 Khronos 回归资产 | 待办 |
| M2-006 | M2 | P1 | Windows 文件拖放 | 待办 |
| M3-001 | M3 | P0 | profile、diagnostic 和 5 条规则 | 待办 |
| M3-002 | M3 | P0 | CLI JSON 和跨入口一致性 | 待办 |
| M4-001 | M4 | P0 | CI 和多目标构建门禁 | 待办 |
| M4-002 | M4 | P0 | benchmark、profiler 和失败路径证据 | 待办 |
| M4-003 | M4 | P0 | Windows/Web `v0.1.0` 发布 | 待办 |
| M5-001 | M5 | P1 | README、演示和项目案例整理 | 待办 |
| FUT-001 | M6 | P2 | macOS/Linux -> Android -> iOS | 待办 |

## 6. 当前风险

| 风险 | 影响 | 缓解方式 |
|---|---|---|
| Windows/Web 的坐标或渲染语义不一致 | 资产显示错误 | 共用内部模型；数值测试与固定视角运行证据 |
| glTF 扩展导致范围膨胀 | v0.1 无法按期收敛 | 只实现明确范围；其余诊断或分类失败 |
| 过早拆 crate 或设计插件 | 时间消耗在无消费者抽象上 | 第二个真实消费者出现后再提取 |
| 损坏 GLB 触发 panic 或过量分配 | 稳定性与安全性问题 | 校验长度/偏移/索引；加入失败和边界测试 |
| WebGPU/Surface 生命周期差异 | Web 或窗口恢复失败 | 保守 features/limits；分别进行真实运行验证 |
| 测试资产许可不清 | 无法公开仓库或演示 | 只使用可追溯资产并保留归属和许可证 |
| 把未验证平台写成已支持 | 项目说明失真 | 只声明真实构建和运行验证结果 |

## 7. 完成定义

任务只有同时满足以下条件才标记完成：

- 相关代码已格式化并通过静态检查。
- 与风险相称的自动化测试通过。
- 验收证据可以复现。
- 用户可见行为、限制或决策已同步到对应文档。
- 没有把后续里程碑能力表述为当前已支持。
